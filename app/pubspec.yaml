name: manaforge_app
description: ManaForge — colección visual y generador de mazos de Magia, multiplataforma.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  forge_engine:
    path: ../forge_engine
  desktop_drop: ^0.4.4
  file_selector: ^1.0.3
  http: ^1.2.0
  image: ^4.2.0
  path: ^1.9.0
  path_provider: ^2.1.3
  sqlite3: ^2.4.0
  sqlite3_flutter_libs: ^0.5.24

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
