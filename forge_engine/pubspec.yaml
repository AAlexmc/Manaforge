name: forge_engine
description: Motor de mazos de ManaForge — curva de maná, validador y generador. Espejo 1:1 de engine-reference (Python).
version: 0.1.0
environment:
  sdk: ^3.0.0
dev_dependencies:
  test: ^1.24.0
